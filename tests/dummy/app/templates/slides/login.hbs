Login here

<button
  onclick={{action 'login' 'sophie' 'screenpassword' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Screen
</button>

<button
  onclick={{action 'login' 'pat' 'presenterpassword' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Presenter
</button>

<button
  onclick={{action 'login' 'alex' '' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 1
</button>

<button
  onclick={{action 'login' 'ben' '' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience 2
</button>
