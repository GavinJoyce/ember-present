Login here

<button
  onclick={{action 'login' 'sophie' 'screen' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Screen
</button>

<button
  onclick={{action 'login' 'pat' 'presenter' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Presenter
</button>

<button
  onclick={{action 'login' 'alex' 'audience' target=session2}}
  disabled={{session.isLoggingIn}}
  class="bg-white hover:bg-blue text-blue-dark hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded"
>
  Audience
</button>