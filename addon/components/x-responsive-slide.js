import Component from '@ember/component';
import layout from '../templates/components/x-responsive-slide2';

export default Component.extend({
  layout,
  classNames: ['slide', 'responsive-slide'],
});
