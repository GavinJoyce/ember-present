{{#if hasBackgroundData}}
  {{x-slide-background backgroundData=backgroundData}}
{{/if}}

{{#if pointerData}}
  {{overlays/x-pointer pointerData=pointerData}}
{{/if}}

{{yield
  (hash
    content=(component 'x-scaled-slide-content')
  )
}}
